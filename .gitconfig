[user]
	name = clalexni
	email = cl.alexni@gmail.com
[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:'%h %ad | %s %C(auto)%d%Creset [%an]' --graph --date=short
  type = cat-file -t
  dump = cat-file -p
